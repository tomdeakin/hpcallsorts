Glossary
========

.. glossary::
   :sorted:

   :abbr:`NUMA (Non-Uniform memory access)`
     A `NUMA`_ system is one where access to different parts of the shared memory address yield different performance characteristics. For example, accessing data in memory which is physically closer to one socket might be faster than access data in memory which is further away.
